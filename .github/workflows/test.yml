name: Manual Test Workflow

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'environment to deploy'
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod
        default: 'dev'

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Test"
